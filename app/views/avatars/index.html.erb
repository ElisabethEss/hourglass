<%# current avatar %>
<%= image_tag("avatars/#{@profile.avatar.name}") %>


<%# All avatars %>
<% Avatar.all.each do |a| %>
  <%= link_to image_tag("avatars/#{a.name}"), user_avatars_path(avatar_id: a.id), data: {turbo_method: :post} %>
<% end %>
